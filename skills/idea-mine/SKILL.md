---
description: "从一组中文/英文论文PDF中挖掘可迁移的研究idea，经审稿人质控后输出paper-init-ready的idea.md（Idea Mining & Evaluation Pipeline）"
---

## 核心使命

读取当前项目 `papers/` 目录下的一组学术论文PDF，围绕用户指定的**目标期刊**，通过"结构化精读 → 期刊画像调研 → idea生成（逐篇迁移评估）→ 主编适配 → 审稿人质控 → 排序输出"六阶段Pipeline，产出可直接灌入 `paper-init` 骨架的 idea.md 内容。

## 核心预设条件

**中文论文 = idea 池，不是已发表文献。** 在整个 pipeline 中，输入的中文/英文论文仅作为 idea 素材来源，不视为已发表的竞争文献。评估创新性时，参照系**只有英文文献**：这个 idea 在英文学术圈有没有人做过？对英文期刊审稿人来说是不是新的？中文论文中已发表的方法/发现不构成"已被做过"的判据。

## 参数解析

使用 AskUserQuestion 询问目标期刊：

读取 `~/.claude/skills/idea-mine/journals.md` 中的期刊池，展示完整列表，让用户输入编号多选（2-5个）：

```
请从以下期刊池中选择 2-5 个目标期刊（输入编号，逗号分隔，如 5,16,17）：

（完整列表从 journals.md 读取，按原始编号展示）
```

记录为 `{JOURNALS}`。

**快捷方式**：如果用户在 `$ARGUMENTS` 中直接传入了期刊编号（如 `/idea-mine 5,16,17`），则跳过交互直接解析。`$ARGUMENTS` 的值为：`$ARGUMENTS`

## 项目目录规范

idea-mine 必须在符合以下标准结构的项目根目录下调用：

```
idea{NN}_{源论文研究领域}/
├── papers/                              ← 【输入】论文PDF池（必须存在）
│   ├── xxx.pdf
│   └── ...
├── paper-notes/                         ← 每篇论文的精读分析报告（自动生成，可缓存复用）
│   ├── xxx.md
│   └── ...
├── idea-raw/                            ← 【中间产物】阶段三原始 idea（自动生成）
│   ├── idea_01.md
│   └── ...
├── idea-adapted/                        ← 【中间产物】阶段四主编适配版本（自动生成）
│   ├── idea_01_{期刊缩写}.md
│   └── ...
├── idea-review/                         ← 【中间产物】阶段五审稿人评审结果（自动生成）
│   ├── idea_01_{期刊缩写}_review.md
│   └── ...
└── idea-output_{DATE}/                  ← 【输出】skill运行后自动生成
    ├── 00_排名总览.md
    ├── 01_idea_xxx.md
    └── ...
```

**命名说明**：
- **项目文件夹名**中的`{源论文研究领域}`描述的是输入论文池的研究主题（如"碳交易+可持续管理"）
- **输出文件夹名**格式为 `idea-output_{DATE}/`
- 论文统一放在 `papers/` 子目录，不要散落在项目根目录
- 输出文件夹生成在项目根目录下，与 `papers/` 同级

## 前置检查

1. 检查 `papers/` 子目录是否存在，不存在则停止并提示用户：`请先创建 papers/ 目录并放入论文PDF`
2. 扫描 `papers/` 子目录中的 **PDF 文件**（`papers/**/*.pdf`）
3. 统计 PDF 数量并列出文件清单
4. 如果 PDF 数量为 0，停止并提示用户放入论文
5. 根据 PDF 数量给出建议：
   - **< 10 篇**：警告 idea 候选空间不足，建议补充至 12 篇以上，尤其注意方法论多样性
   - **10-20 篇**：甜蜜点（15篇左右最优），直接运行
   - **21-25 篇**：可以运行，提示"论文较多，注意力可能摊薄"
   - **> 25 篇**：建议用户分两批运行（每批 12-18 篇），或自行预筛至 20 篇以内。如用户坚持一次跑完，则执行但警告质量可能下降
6. 向用户确认：`发现 N 篇论文，目标期刊：{JOURNALS}。开始挖掘？`

## Pipeline 执行

严格按以下六个阶段顺序执行。

---

### 阶段一：结构化精读（并行 subagents）

**目标**：提取每篇论文的可迁移idea内核。

**缓存机制**：精读结果存储在项目根目录的 `paper-notes/` 下，每篇论文一个 md 文件（文件名 = PDF 文件名去掉 `.pdf` 后缀）。论文内容不会变，**只要 md 存在就直接复用，不需要重新精读**。

对每篇 PDF，先检查 `paper-notes/{论文名}.md` 是否存在：
- 存在 → 直接读取，跳过精读
- 不存在 → 纳入精读队列

每篇 PDF 启动 **1 个独立的 Agent**（subagent_type: general-purpose），1 篇 / 1 agent，避免单 agent 任务过重导致超时或失败时多篇连坐。采用**槽位制并发控制**：同时运行的 agent 数**不超过 8 个**，每当 1 个 agent 完成（或失败重试后仍失败），立即从队列中取下一篇启动，直到所有论文处理完毕。不等整批完成再启动下一批。每个 Agent 的 prompt 如下：

```
你是一名管理科学与工程方向的高级研究助理。请全文精读以下PDF论文，对每篇论文提取以下信息：

1. **一句话核心idea**：剥离具体行业情境后，这篇论文的可迁移学术贡献是什么？
2. **方法论标签**：博弈论/演化博弈/实证DID/面板回归/优化/预测/综述/DSGE/微分博弈/...
3. **核心机制标签**：倒U型/溢出效应/信息不对称/政策协同/委托代理/竞合博弈/...
4. **理论框架**：使用了什么理论？（如Weitzman框架、Hotelling模型、委托代理等）
5. **关键发现**（3条以内，每条展开到3-5句，必须包含具体的变量关系、效应大小/方向、非线性形式、边界条件等细节）
6. **核心创新点**（2-3个，每个展开说明：创新在哪里、为什么是创新、对跨领域迁移有什么价值。区分理论创新、方法创新、发现创新）
7. **研究情境/行业背景**：这篇论文研究的是什么行业/领域的什么问题？（2-3句话，帮助判断情境迁移的可行性）
8. **模型/方法的关键假设**：逐条列出每个核心假设（博弈论的参与者/策略/支付假设、实证的识别策略/排他性约束、优化的目标函数/约束条件等），并分析每个假设在跨领域迁移后是否仍然成立
9. **数据来源与样本特征**：用了什么数据、样本量多大、时间跨度、数据库来源（帮助评估可行性）
10. **原论文 idea 逻辑链**：用5-8句连贯叙述串联"实践痛点 → 文献gap → 研究目标 → RQ → 方法选择 → 核心发现 → 贡献声明"的完整逻辑链（不要分点列，要体现逻辑链的连贯性，每个环节的逻辑交代充分，帮助判断迁移后 idea 该怎么重构）

每项信息**宁可详细不可遗漏**。关键发现、核心创新点、关键假设三项尤其要充分展开，这些是后续 idea 迁移的核心素材。请逐字逐句仔细阅读，不要遗漏任何重要细节。

将每篇论文的精读结果直接写入 paper-notes/{论文名去掉.pdf后缀}.md，格式见下方模板。如果 paper-notes/ 目录不存在则先创建。

论文列表（路径来自前置检查扫描 papers/ 的结果，均带 papers/ 前缀）：
[论文路径列表]
```

**精读结果写入**：每篇论文的精读结果写入 `paper-notes/{论文名}.md`，格式：

```markdown
---
paper: {论文完整文件名}
analyzed: {YYYY-MM-DD}
---

## 1. 一句话核心 idea
{内容}

## 2. 方法论标签
{内容}

## 3. 核心机制标签
{内容}

## 4. 理论框架
{内容}

## 5. 关键发现
{每条3-5句，含具体变量关系、效应、边界条件}

## 6. 核心创新点
{每个展开说明创新在哪、为什么、迁移价值}

## 7. 研究情境/行业背景
{内容}

## 8. 模型/方法的关键假设
{逐条列出，分析迁移后是否成立}

## 9. 数据来源与样本特征
{内容}

## 10. 原论文 idea 逻辑链
{5-8句连贯叙述}
```

**失败处理**：如果某个 subagent 返回错误（如 API 连接失败），自动重试一次。重试仍失败则跳过该篇（该论文的 paper-notes/ md 不生成），提醒用户。Pipeline 继续执行（已成功精读的论文仍参与后续阶段）。

**汇总**：`paper-notes/` 下的所有 md 文件（含缓存复用的和新精读的）即为**论文摘要表**，供后续阶段直接读取使用，此处不在对话框中展示给用户（最终会汇总写入阶段六的 `00_排名总览.md`）。

**向用户展示方法论分布统计**，让用户确认论文池的方法论覆盖度：

```markdown
## 论文池方法论分布

| 方法论类型 | 篇数 | 论文 |
|-----------|------|------|
| 博弈论/机制设计 | N | 论文A, 论文B, ... |
| 实证因果推断(DID/IV) | N | ... |
| 演化博弈/仿真 | N | ... |
| 优化/预测 | N | ... |
| 综述/框架 | N | ... |
| 其他 | N | ... |
```

仅展示分布统计，不做判断性提醒（用户可能故意选同类方法论文来做创新点互补）。

---

### 阶段二：目标期刊综合画像调研

在正式生成 idea 之前，获取 `{JOURNALS}` 中每个期刊的综合画像：

**缓存机制**：期刊画像存储在 `~/Library/CloudStorage/Dropbox/02-Research/Zylen paper/_Zylen idea/_journal-spec/` 目录下，每本期刊一个 md 文件（文件名 = 期刊全称，如 `Journal of Construction Engineering and Management.md`），跨项目共享复用。

对每本选中期刊，执行以下检查：
1. 检查 `_journal-spec/{期刊全称}.md` 是否存在
2. 如果存在，读取 frontmatter 中的 `updated` 日期：
   - 距今 **≤ 30 天** → 直接复用，跳过 WebSearch，输出提示：`{期刊名} 画像缓存命中（{updated}），跳过调研`
   - 距今 **> 30 天** → 重新调研，覆盖写入
3. 如果不存在 → 新建调研，写入 `_journal-spec/{期刊全称}.md`

每个需要生成画像（新建或过期）的期刊启动独立的 subagent（subagent_type: general-purpose），用 WebSearch 获取 Aims & Scope + 基于模型知识补充其余 3 项。已缓存的期刊直接读取文件。

**失败处理**：如果某期刊的 WebSearch 未命中 Aims & Scope，则基于模型知识直接输出（在画像中标注"来源：模型知识，WebSearch 未命中"），不阻塞 pipeline。

**画像内容**（每个期刊 4 项，来源不同）：

| # | 内容 | 来源 |
|---|------|------|
| 1 | **Aims & Scope** | WebSearch 官网 |
| 2 | **研究主题热点** | 模型知识（可选搜索验证） |
| 3 | **方法论接收范围** | 模型知识（可选搜索验证） |
| 4 | **期刊审美偏好** | 模型知识（该期刊特别看重什么？如 JCLP 看重 policy relevance，JCEM 要求 construction industry 特殊性论证） |

**搜索策略**（仅用于 Aims & Scope）：
- `"{期刊名}" aims and scope`

其余 3 项基于模型对该期刊的理解直接输出，如有必要可补充搜索验证。

**画像文件写入**：每次生成后（新建或过期重新生成），将画像内容写入 `_journal-spec/{期刊全称}.md`，frontmatter 中 `updated` 设为当前日期。文件格式：

```markdown
---
journal: {期刊全称}
updated: {YYYY-MM-DD}
---

## Aims & Scope
{来源：WebSearch 官网}

## 研究主题热点
{来源：模型知识，可选搜索验证}
1. {主题}
2. {主题}
3. {主题}

## 方法论接收范围
{来源：模型知识，可选搜索验证}
{列出该期刊接收的方法论类型}

## 期刊审美偏好
{来源：模型知识}
{该期刊特别看重什么？审稿人最在意什么？}
```

**此画像的用途**：
- **不约束方法论选择**（方法跟随主论文）
- **在阶段四中指导主编适配**：每个期刊的主编 subagent 参考本期刊画像，将原始 idea 做完整的期刊适配
- 在阶段五中供各审稿人参考：审稿人根据自己期刊的画像评判适配版本的期刊适配度
- 同时用于动态生成主编和审稿人的人设（特殊关注点来自画像中的"期刊审美偏好"）

---

### 阶段三：Idea 生成（识别可迁移内核）

**目标**：从论文池中识别具有跨领域迁移价值的 idea 内核，经排序筛选后取 **top 10** 进入阶段四。

> **执行方式**：Step 1 由主 agent 执行，Step 2 由 subagent 并行执行，Step 3 由主 agent 汇总。不使用 WebSearch 做前置检索——WebSearch 能搜到的主要是开源期刊（Frontiers、MDPI、PLOS ONE 等），质量远低于目标期刊；闭源高质量期刊的全文和摘要无法通过 WebSearch 获取。直接**依赖模型知识**判断各 idea 在英文学术圈的研究密度和新颖性。阶段二已获取的期刊画像（Aims & Scope、研究主题热点、方法论接收范围、审美偏好）提供了充分的期刊定位信息。

#### Step 1：逐篇迁移价值评估（主 agent 执行）

逐篇扫描 `paper-notes/` 下的精读报告，判断每篇论文的核心 idea 是否具备**独立的跨领域迁移价值**。

判断标准：
- idea 内核在英文学术圈确实未被充分探索（**参照系仅为英文文献**，中文论文是 idea 池不构成"已被做过"）
- 中文→英文跨语言迁移不存在抄袭问题，不需要担心这一点
- 只需评判：**idea 内核的学术贡献是否有跨领域迁移价值**、**方法论在其他领域是否可行**（具体期刊适配在阶段四由主编处理，此步不评）

**硬筛选规则**（直接标记为不进入 Step 2，无需评估）：
- 综述类论文（如方法论综述、框架综述）→ 标记为 📚 综述
- 以 DID（双重差分）或 RDD（断点回归）为核心方法的论文 → 标记为 ❌，理由："DID/RDD 方法不采用"

产出：**主 idea 候选清单**，每篇标注状态和理由，写入 `idea-raw/_step1_candidates.md`：

```markdown
| # | 论文 | 状态 | 理由 |
|---|------|------|------|
| P1 | {论文名} | ✅ 值得迁移 | {简述迁移价值} |
| P2 | {论文名} | ❌ 不值得迁移 | {简述原因} |
| P3 | {论文名} | ⚠️ 存疑 | {简述疑虑} |
| P4 | {论文名} | 📚 综述 | {不作为独立idea源} |
```

**各状态在 Step 2 中的处理**：
- ✅ 值得迁移 → 启动 subagent 进入 Step 2
- ⚠️ 存疑 → **也启动 subagent**（通过评分来验证——如果评分低则自然被 Step 3 淘汰，不需要提前排除）
- ❌ 不值得迁移 → 不进入 Step 2
- 📚 综述 → 不进入 Step 2

#### Step 2：逐篇迁移评估 + 评分（subagent 并行执行）

对 Step 1 中标注为 ✅ 或 ⚠️ 的每篇论文，启动 **1 个独立的 Agent**（subagent_type: general-purpose）。采用**槽位制并发控制**：同时运行的 agent 数**不超过 8 个**，完成一个补一个。

每个 subagent 的 prompt 包含：
- 该主论文的 `paper-notes/{论文名}.md` 的完整绝对路径（请用 Read 工具读取）

**每个 subagent 的工作流**：

```
1. 读取自己负责的主论文的完整 paper-notes
2. 基于该论文的 idea 内核，独立构建一个完整的跨领域迁移 idea：
   - 将 idea 内核从原论文的行业情境中剥离
   - 构建通用版本的 §1-§5（行业背景→gap→RQ→方法→贡献）
   - 评估方法论跨领域迁移后的可行性（核心假设是否仍然成立）
3. 做评分（评分维度见下方）
4. 将结果写入 idea-raw/idea_{NN}.md（NN 为主论文在 Step 1 清单中的编号）
```

每个 subagent 独立评分（它对该 idea 的理解最深）。

##### 评分维度

> **注意**：此为 Pipeline 内部的快速初筛评分（3维度/25分制），目的是从候选中高效筛选 top 10。与阶段五审稿人的五维度精评（1-10分/80分制）是完全独立的两套评分体系——初筛在前，精评在后，互不影响。

| 维度 | 分值 | 权重 | 说明 |
|------|------|------|------|
| 创新性 | 1-5 | ×2 | 该 idea 在英文学术圈有多新 |
| 贡献 | 1-5 | ×2 | 对领域知识体系的理论/实践推进 |
| 落地可行性 | 1-5 | ×1 | 数据能否获取、方法能否落地 |

**总分** = 创新性×2 + 贡献×2 + 落地可行性×1，满分 25

#### Step 3：汇总排序筛选（主 agent 执行）

所有 subagent 完成后，主 agent 读取 `idea-raw/idea_*.md` 中的所有候选，执行以下**纯机械操作**：

1. 读取每个文件的评分
2. 按总分降序排列，取 **top 10** 进入阶段四主编适配。如总数不足 10，全部进入
3. **不在对话框中输出 idea 全文**，仅展示排名表摘要（编号 + 一句话标题 + 总分）

**失败处理**：
- Step 1 为主 agent 同步执行，不涉及 subagent 失败
- Step 2 subagent 失败（如 API 连接失败）→ 自动重试一次。重试仍失败则跳过该篇主论文（对应的 idea-raw/idea_{NN}.md 不生成），提醒用户。已成功的 subagent 结果不受影响
- Step 3 为纯机械汇总，不会失败

#### 迁移创新性判断标准（全局适用）

所有 idea 都涉及跨领域迁移，统一适用以下判断（**参照系仅为英文文献**，中文论文是 idea 池不构成"已被做过"）：
- idea 内核在英文学术圈确实未被充分探索（以英文文献为准）
- **情境差异能产生与原论文不同的理论预测** → 创新性升级
- **理论预测方向与原论文一致** → 创新性降级（但迁移中文→英文本身有价值，体现在评分中）
- 有足够的数据/案例支撑落地（具体领域的数据可行性在阶段四由主编评估）

以上为定性判断标准。定量评分在 Step 2 各 subagent 内执行（创新性×2 + 贡献×2 + 落地可行性×1）。

#### 每个 idea 的输出格式

每个 Step 2 subagent 将其负责的 idea 写入 `idea-raw/idea_{NN}.md`（如 idea-raw/ 不存在则先创建）。严格按照 idea.md 的 §1-§5 标准结构，附加评分和元数据。

```markdown
---
idea: {NN}
主论文: {论文名}
---

### Idea {NN}: {一句话标题}

> **目标期刊**: {JOURNALS}

#### 1. 灵感来源 & 参考论文
- **主论文**：{论文名} — {一句话核心 idea}

#### 2. 行业背景与实践难题 → 文献不足 → Gap/Objective/RQ

**行业背景与实践难题**：{基于主论文的通用描述（具体期刊适配在阶段四）}

**文献不足**：{现有研究的不足，基于主论文的通用判断（具体期刊适配在阶段四）}

| Gap | Objective | RQ |
|:----|:----------|:---|
| G1: {gap} | {objective} | RQ1: {rq} |
| G2: {gap} | {objective} | RQ2: {rq} |

#### 3. 方法论选择论证
{为什么选择这个方法？核心假设在跨领域迁移后是否成立？}

#### 4. 贡献意图
**理论贡献**：{预期理论推进}
**实践贡献**：{预期实践价值}

#### 5. 风险评估
TODO（阶段五审稿人 challenge 产出后回填）

---
**评分**：
- 创新性：{X}/5 | 贡献：{X}/5 | 落地可行性：{X}/5
- **总分**：{X}/25

**附加元数据**（idea-mine 专用，不属于 idea.md 核心结构）：
- **创新逻辑**：{100-150字，说清楚主论文的 idea 内核是什么、为什么迁移到英文期刊后有新价值}
- **预期核心发现/命题**：{2-3条，让审稿人能判断"做出来会是什么样"}
- **数据/案例可行性**：{展开说明——数据从哪来、样本怎么构建、有没有现成的数据库}
```

**注意**：原始 idea 是基于论文池的通用版本（§1-§5 结构），尚未针对具体期刊适配。§5 风险评估在阶段五审稿人 challenge 产出后回填。期刊适配在阶段四由主编完成。

**方法论选择的决策优先级**：
1. **主论文方法**（最高优先级）→ 用户选中的论文代表用户认可其方法，idea 的主框架方法跟随主论文
2. **数据/模型可行性** → 该方法跨领域迁移后能否落地（数据可得性、建模条件）。如果可行性存疑，不换方法，而是在可行性维度扣分

---

### 阶段四：主编适配（并行 subagents）

**目标**：将每个原始 idea 面向每个目标期刊做完整适配，每刊出 2 个差异化的适配版本。

**实现方式**：每个 idea × 每个期刊 = 1 个独立的主编 Agent（subagent_type: general-purpose），扮演该期刊的主编（Editor-in-Chief）。每个主编 agent 只负责将 **1 个** 原始 idea 适配到自己的期刊（2 个差异化版本），并将结果写入文件。采用**槽位制并发控制**：同时运行的 agent 数**不超过 8 个**，完成一个补一个。共需启动 M × N 个 agent（M = 实际进入阶段四的 idea 数，最多 10；N = 用户选中的期刊数）。

主编的人设根据阶段二的期刊画像生成。prompt 模板：

```
你现在是 {期刊名} 的主编（Editor-in-Chief）。你深谙本刊的读者群体、
学术定位和选题偏好，清楚什么样的研究问题和叙事角度最能打动你的审稿人。

你的任务：将 1 个原始 idea 适配到你的期刊，使其成为一个完整的、
有竞争力的投稿方案。适配不是简单换个说法——你需要重新思考：
- 研究问题该怎么表述才能引起本刊读者的共鸣
- 从什么实践痛点切入最能体现本刊的关注领域
- 文献 gap 从哪个维度框定最符合本刊的学术定位
- 贡献声明该强调什么才能满足本刊的期待（理论/实践/政策）

**请先用 Read 工具读取以下文件**：
1. 本刊画像：{_journal-spec/{期刊全称}.md 的完整绝对路径}
2. 原始 idea：{idea-raw/idea_{NN}.md 的完整绝对路径}

请生成 2 个差异化的适配版本。2 个版本之间必须有本质不同
（不能只是换个说法），可以在以下维度产生差异：
- 切入的实践痛点不同
- 研究问题的表述角度不同
- gap 的框定维度不同
- 贡献声明的侧重不同（理论 vs 实践 vs 政策）

输出格式要求：每个适配版本必须严格按照 §1-§5 结构输出：
§1 灵感来源 & 参考论文
§2 行业背景与实践难题 → 文献不足 → Gap/Objective/RQ（含 Gap-Objective-RQ 表格）
§3 方法论选择论证
§4 贡献意图（理论贡献 + 实践贡献）
§5 风险评估（本阶段标注 TODO，由审稿人后续回填）
附加元数据：与原始 idea 的差异说明 + 完整叙事链

**请将 2 个适配版本写入以下文件**：
{idea-adapted/idea_{NN}_{期刊缩写}.md 的完整绝对路径}
```

**每个适配版本的输出格式**（严格遵循 §1-§5 标准结构）：

```markdown
### Idea {N} — {期刊名} 适配版本 {A/B}

> **目标期刊**: {期刊名}

#### 1. 灵感来源 & 参考论文
- **主论文**：{继承自原始 idea，说明为什么适合本刊}

#### 2. 行业背景与实践难题 → 文献不足 → Gap/Objective/RQ

**行业背景与实践难题**：{为本期刊读者重新表述的实践痛点}

**文献不足**：{从本期刊 scope 出发的 gap 框定}

| Gap | Objective | RQ |
|:----|:----------|:---|
| G1: {为本期刊重新框定的gap} | {objective} | RQ1: {为本期刊重新表述的RQ} |
| G2: ... | ... | RQ2: ... |

#### 3. 方法论选择论证
{与原始 idea 相同的方法，但说明为什么适合本刊}

#### 4. 贡献意图
**理论贡献**：{为本期刊读者量身定制的理论贡献声明}
**实践贡献**：{为本期刊读者量身定制的实践贡献声明}

#### 5. 风险评估
TODO（阶段五审稿人 challenge 产出后回填）

---
**附加元数据**（适配阶段专用，内容与阶段三的附加元数据不同——阶段三记录的是迁移逻辑，此处记录的是适配说明）：
- **与原始 idea 的差异说明**：{适配了什么、为什么这样适配}
- **完整叙事链**：{§2痛点 → §2gap → §2RQ → §3方法 → 预期发现 → §4贡献，一段话串联}
```

**产出**：M 个原始 idea × N 个期刊 × 2 个版本 = 全部适配版本，存储在 `idea-adapted/` 目录下（每个文件含版本 A/B），进入阶段五审稿。

---

### 阶段五：审稿人质量控制

**目标**：以目标期刊审稿人身份对每个 idea 做严格评估。

#### 角色设定（多审稿人，动态适配目标期刊）

每个 idea 由用户选中的**所有期刊（2-5个）**的审稿人分别独立评审（多视角评审）。用户选了几个期刊就启动几个审稿人，全覆盖，不做预选。

**实现方式**：**槽位制并发控制**——每个 idea × 每个期刊 = 1 个独立 Agent（subagent_type: general-purpose），每个 Agent 扮演对应期刊的审稿人。同时运行的 agent 总数**不超过 8 个**，每当 1 个 agent 完成，立即从队列中取下一个（idea × 期刊）组合启动，直到所有评审完成。不等整批完成再启动下一批。所有 Agent **互不知晓其他审稿人的评分结果**，确保独立性。

每个审稿人的人设根据阶段二的期刊画像**独立生成**（关注点不同）。prompt 模板：

```
你现在是 {期刊名} 的资深审稿人（Associate Editor 级别）。你见过太多
"换个情境重做一遍"的投稿，对真正的创新有极高的辨别力。

重要预设：你评审的这些 idea 来源于一个非公开的 idea 池（不是已发表文献），
因此不存在"这个已经被做过"的问题。你只需从英文学术圈的视角判断这个 idea
是否有创新性和贡献。

作为该期刊的审稿人，你特别关注：
{根据该期刊画像动态生成 2-3 条特殊关注点，例如：}
{JCLP: practical implications 是否具体可落地、policy relevance、环境效益是否可量化}
{JCEM: construction industry 特殊性论证、工程数据来源可信度、对实践者的决策建议}
{SCS: 城市尺度的数据和案例、与可持续发展目标(SDGs)的关联、跨学科视角}

**请先用 Read 工具读取以下文件**：
1. 本刊画像：{_journal-spec/{期刊全称}.md 的完整绝对路径}
2. 待评审的适配版本（含版本 A/B）：{idea-adapted/idea_{NN}_{期刊缩写}.md 的完整绝对路径}
3. 原始 idea 附加元数据（预期核心发现、数据可行性等）：{idea-raw/idea_{NN}.md 的完整绝对路径}

读取后，你需要：
1. 逐一阅读 2 个适配版本，选出最推荐的版本并说明理由
2. 对 2 个版本都做完整的五维度评分
3. 对落选版本点评：弱在哪里？有没有可取之处？
4. 审稿人最可能 challenge 的 1-2 个核心问题是什么？
5. 作者如何预防性地回应这些 challenge？

五维度评分 rubric（每项 1-10 分，2 个版本各打一次）：
| 维度 | 1-3分 | 4-6分 | 7-10分 |
| 创新性 | 简单换情境/拼凑 | 有一定新意但非突破 | 打开新的研究方向 |
| 贡献 | 对理论和实践都没有明确推进 | 拓展了现有理论框架或有一定实践指导意义 | 理论突破+实践价值兼备 |
| 方法可行性 | 数据不可得/模型不可解 | 有挑战但可克服 | 数据现成/方法成熟 |
| 时效性 | 过时话题 | 常规热点 | 踩中政策窗口/前沿热点 |
| 发表概率 | <10% | 30-50% | >70% |

**请将评审结果写入以下文件**：
{idea-review/idea_{NN}_{期刊缩写}_review.md 的完整绝对路径}
```

#### 五维度评分（每项 1-10 分，2 个适配版本各打一次）

| 维度 | 1-3分 | 4-6分 | 7-10分 |
|------|-------|-------|--------|
| **创新性** (Innovation) | 简单换情境/拼凑 | 有一定新意但非突破 | 打开新的研究方向 |
| **贡献** (Contribution) | 对理论和实践都没有明确推进 | 拓展了现有理论框架或有一定实践指导意义 | 理论突破+实践价值兼备 |
| **方法可行性** (Feasibility) | 数据不可得/模型不可解 | 有挑战但可克服 | 数据现成/方法成熟 |
| **时效性** (Timeliness) | 过时话题 | 常规热点 | 踩中政策窗口/前沿热点 |
| **发表概率** (Publishability) | <10% | 30-50% | >70% |

> **创新性评分注意**：参照系仅为英文文献，中文论文作为 idea 池不构成"已被做过"的判据。

#### 每个 idea 的评估流程

**第一步：多审稿人量化评分**（N 个独立 subagent 各自完成，互不知晓其他审稿人结果）

每个审稿人对该 idea 的 2 个适配版本（A/B）逐一做五维度评分，选出推荐版本。综合分由主 agent 根据五维度评分加权计算，审稿人不需要自己算。

**第二步：六维度定性评估报告**（基于所有审稿人的评分结果综合撰写）

按递进关系组织六个维度，定性报告**必须与量化评分保持一致**——如各审稿人评分有分歧，定性报告必须解释分歧的原因。

**第三步：期刊推荐结论**

**第四步：§5 风险评估回填**（此步由阶段六文件生成 subagent 统一执行，非审稿人 subagent）

审稿人 Challenge 产出后，将推荐适配版本的 §5 风险评估回填：
- 每个 challenge 作为一行风险
- 等级根据审稿人判断标注（⚠️中 / 🔴高）
- 应对策略来自审稿人的预防性回应建议

#### 每个 idea 的独立 md 文件结构

每个 idea 生成一个独立 md 文件（每个 idea 启动 1 个独立 subagent 并行生成，槽位制并发），结构如下：

```markdown
# Idea {N}: {标题}

## 基本信息（§1-§5 摘要）

> **目标期刊**: {JOURNALS}

- **§1 灵感来源**：{主论文citation}
- **§2 研究问题**：{RQ 概述}
- **§3 方法论**：{方法}
- **§4 贡献意图**：{理论+实践贡献概述}
- **§5 风险评估**：见审稿人 Challenge

- **各期刊适配版本摘要**：
  - **{期刊1}**：共 2 个版本，审稿人推荐版本 {A/B}
  - **{期刊2}**：共 2 个版本，审稿人推荐版本 {A/B}
  - ...（详见各审稿人评分表）

---

## 适配版本全文

> 阶段四各期刊主编产出的完整适配版本，供用户对比不同期刊的叙事角度和 RQ 表述。

### {期刊1}

#### 版本 A

{完整 §1-§5 内容，来自阶段四该期刊主编 agent 的输出}

#### 版本 B

{完整 §1-§5 内容}

### {期刊2}

#### 版本 A

{完整 §1-§5 内容}

#### 版本 B

{完整 §1-§5 内容}

（每个选中期刊各 2 个版本，全部保留）

---

## 第一部分：多审稿人量化评分

> 为用户选中的每个期刊各输出一个评分表（格式相同），以下为单个审稿人评分表模板：

### {期刊名} 审稿人

**推荐版本**：**版本 {A/B}** | 理由：{为什么这个版本最适合本期刊}

**两版本评分对比**：

| 维度 | 版本A | 版本B |
|------|-------|-------|
| 创新性 | {X}/10 | {X}/10 |
| 贡献 | {X}/10 | {X}/10 |
| 方法可行性 | {X}/10 | {X}/10 |
| 时效性 | {X}/10 | {X}/10 |
| 发表概率 | {X}/10 | {X}/10 |
| **综合分** | {X}/80 | {X}/80 |

> 综合分由主 agent 根据五维度评分加权计算：创新×2 + 贡献×2 + 方法可行性×1.5 + 时效×1 + 发表概率×1.5，满分 80。审稿人只需给出五维度分数，不需要自己算综合分。

**落选版本点评**：
- 版本{X}：{弱在哪里？可取之处？}

**综合评分**（基于推荐版本）：{X}/80

（重复以上模板，每个选中期刊各一个评分表）

### 平均综合评分：所有审稿人综合评分的平均值 = {X}/80

### 审稿人 Challenge（合并所有审稿人视角）

1. {具体质疑点}
2. {具体质疑点}

**预防性回应策略**：
1. {如何在论文中提前化解}
2. {如何在论文中提前化解}

**改进建议**：{如何调整使其更强}

---

## 第二部分：六维度定性评估报告

> 以下六个维度按递进关系排列：情境适配度是前提 → 实践嵌入度是根基 → 叙事完整度是骨架 → 贡献程度是结果 → 创新程度是价值 → 可行性是落地保障。
> 定性评估基于第一部分的多审稿人量化评分综合撰写，不能与量化评分存在矛盾。

### 一、情境适配度评估（前提）

> 参考：各审稿人的发表概率分 + 方法可行性分

- **期刊 scope 匹配度**：{详细文字分析}
- **迁移后是否产生不同的理论预测**：{详细文字分析}
- **该领域数据/案例可得性**：{详细文字分析}

### 二、实践嵌入度评估（根基）

> 参考：各审稿人的贡献分（实践部分）

- **对应的实践难题**：{用一句大白话描述，不用学术语言}
- **难题真实性**：{真实存在/存疑}，依据：{行业报告/政策文件/新闻事件/企业案例}
- **实践者会关心吗**：{会/不确定/不会}，{详细理由}
- **结论可转化性**：{能直接转化为实践建议 / 需进一步落地 / 纯学术意义}

### 三、叙事完整度评估（骨架）

> 基于各审稿人推荐的适配版本做逻辑链检测。如不同期刊审稿人推荐了不同的版本，分别说明。

**推荐版本逻辑链检测**：

| 衔接环节 | 状态 | 说明 |
|---------|------|------|
| 实践痛点 → 文献gap | {通畅/有跳跃/断裂} | {一句话} |
| 文献gap → RQ | {通畅/有跳跃/断裂} | {一句话} |
| RQ → 方法选择 | {通畅/有跳跃/断裂} | {一句话} |
| 方法 → 预期发现 | {通畅/有跳跃/断裂} | {一句话} |
| 预期发现 → 贡献回扣 | {通畅/有跳跃/断裂} | {一句话} |

**最薄弱环节**：{指出}
**修补建议**：{如何补强}

**落选版本简评**：{审稿人对落选适配版本的关键评价}

### 四、贡献程度评估（结果）

> 参考：各审稿人的贡献分

- **理论贡献**：{对哪个理论/框架有什么推进，详细分析}
- **实践贡献**：{对哪类决策者有什么指导，详细分析}
- **方法贡献**：{是否有方法论层面的推进}
- **在该期刊学术领域中的定位**：{填补了什么空白}

### 五、创新程度评估（价值）

> 参考：各审稿人的创新性分
> **参照系仅为英文文献**——中文论文是 idea 池不是竞争文献

- **方法创新**：{详细分析}
- **理论创新**：{详细分析}
- **研究视角创新**：{详细分析}
- **各审稿人评分分歧说明**：{如有分歧，解释原因；如一致，说明共识点}

### 六、可行性评估（落地）

> 参考：各审稿人的方法可行性分

- **数据可得性**：{公开数据库？问卷？企业合作？详细分析}
- **模型构建条件**：{假设在该期刊覆盖领域是否合理？变量能否度量？}
- **案例可得性**：{如果需要案例，该期刊覆盖领域有没有合适的？}

---

## 期刊推荐

- **更推荐**：{期刊X}
- **理由**：{基于所有审稿人评分的综合比较}

---

## idea.md

{paper-init-ready 的完整 idea.md 内容，格式严格遵循 paper-init 的 idea.md 模板}
```

---

### 阶段六：排序输出 + 独立 md 文件

**目标**：在对话框展示排名表，同时为每个 idea 生成独立 md 文件到统一文件夹。

#### 6.1 对话框输出：排名表

在对话框中展示排名表供用户快速浏览：

```markdown
## 最终排名

| 排名 | Idea | {期刊1}综合 | {期刊2}综合 | ... | 平均综合 | 推荐期刊 |
|------|------|-----------|-----------|-----|---------|---------|
| 1 | ... | X/80 | X/80 | ... | X/80 | {期刊X} |
| ... | ... | ... | ... | ... | ... | ... |
```

**说明**：综合分 = 创新×2 + 贡献×2 + 可行×1.5 + 时效×1 + 发表×1.5，满分80。取审稿人推荐版本的评分。平均综合 = 所有审稿人综合分的平均值。默认按平均综合分降序排列，但用户可自行按某个期刊的分数或分项维度判断优先级。各 idea 的详细评估报告见独立 md 文件。

#### 6.2 文件夹输出：独立 md 文件

在**项目根目录**下创建文件夹（与 `papers/` 同级），每个 idea 一个独立 md 文件：

```
idea-output_{DATE}/
├── 00_排名总览.md              ← 排名表 + 论文摘要表 + 方法论分布 + 期刊画像
├── 01_idea_{标题简称}.md       ← idea 1 的完整评估报告（结构见阶段五）
├── 02_idea_{标题简称}.md       ← idea 2 的完整评估报告
├── ...
└── {N}_idea_{标题简称}.md
```

**00_排名总览.md** 包含：论文摘要表、方法论分布统计、阶段二的期刊画像、最终排名表。

**论文摘要表格式**（汇总自 `paper-notes/`，每篇论文一行）：

```markdown
| # | 论文 | 方法论标签 | 核心机制标签 | 理论框架 | 研究情境 | 一句话核心 idea |
|---|------|-----------|-------------|---------|---------|---------------|
| 1 | {论文名} | {标签} | {标签} | {框架} | {情境} | {一句话} |
| ... | ... | ... | ... | ... | ... | ... |
```

**每个 idea 的 md 文件**按阶段五定义的结构输出（基本信息 → 适配版本全文 → 多审稿人量化评分 → 六维度定性报告 → 期刊推荐 → idea.md）。

**idea.md 部分的关键约束**：
- 使用期刊推荐结论中"更推荐"期刊的审稿人推荐适配版本的 §1-§5 内容，§5 已回填 challenge（用户在阶段 6.3 落地时可按实际选择的期刊覆盖）
- 格式严格遵循 paper-init 的 idea.md 模板（§1灵感来源 §2背景→gap→RQ §3方法论 §4贡献 §5风险），标题行写 `# 研究纲领文件`（不含 {ID}，6.3 落地覆盖时替换为 `# {ID} 研究纲领文件`）。header 填写：版本 `v1.0`（已过审稿人质控）、日期为当天、目标期刊填推荐期刊、"定位"行保留模板原文
- Gap/RQ 表必须有 2-3 行，不能只有 1 行
- §5 风险评估表必须包含阶段五审稿人识别的 challenge
- 附加元数据（创新逻辑、预期发现等）不写入 idea.md，仅保留在评估报告 md 中

**生成方式**：每个 idea 启动 **1 个独立的 subagent** 生成其 md 文件，采用**槽位制并发控制**（同时不超过 8 个），完成一个补一个。subagent 通过 Read 工具自行读取所需的中间文件，主 agent 只在 prompt 中提供文件路径。

**subagent 需读取的文件**（主 agent 在 prompt 中提供完整绝对路径）：
- `idea-raw/idea_{NN}.md`（原始 idea 的 §1-§5 + 附加元数据）
- `idea-adapted/idea_{NN}_*.md`（该 idea 在所有期刊的适配版本，用 Glob 工具匹配）
- `idea-review/idea_{NN}_*_review.md`（该 idea 在所有期刊的审稿人评审结果，用 Glob 工具匹配）
- `_journal-spec/*.md`（所有期刊画像，用 Glob 工具匹配）

**文件生成 subagent 的职责**：基于上述原始数据，独立完成以下工作并输出完整的 per-idea md 文件：
1. 根据五维度评分加权计算综合分（创新×2 + 贡献×2 + 方法可行性×1.5 + 时效×1 + 发表概率×1.5）
2. 撰写六维度定性评估报告（必须与量化评分保持一致）
3. 给出期刊推荐结论（基于各审稿人综合分比较）
4. 回填 §5 风险评估（将 challenge 转化为风险表）
5. 生成末尾的 paper-init-ready idea.md 内容

#### 6.3 下一步操作建议（自动衔接 paper-init）

输出完成后，提示用户：
```
请选择一个 idea（输入排名编号），并告诉我：
1. 项目路径（如 ~/Dropbox/02-Research/zl16）
2. 论文编号（如 zl16）
```

用户选择后，根据该 idea 的评估结果**预填默认参数**，让用户确认：
```
根据该 idea 的评估结果，预填以下参数：
- 出版社：{根据推荐期刊的 @publisher 标签从 journals.md 自动推断}
- 目标期刊：{期刊推荐结论中最推荐的期刊}
- 方法类型：请选择（paper-init 会在初始化时询问）

请确认或修改以上参数。
```

**出版社推断规则**：
- 读取 journals.md 中推荐期刊的 `@publisher` 标签
- paper-init 支持的六种出版社：`@elsevier`/`@asce`/`@emerald`/`@sage`/`@ieee`/`@wiley`，直接预填
- 如果未来出现不在以上六种中的标签，提示用户手动选择

用户确认后，自动执行：
```
① 如果目标路径不存在，先 mkdir -p 创建
② cd 到该路径
③ 运行 /paper-init {编号}
   → paper-init 会依次询问出版社和方法类型：
     · 提示用户在 paper-init 交互中按建议选择 {PUBLISHER}
     · 方法类型由用户在 paper-init 交互中自行选择
④ paper-init 完全成功后（编译验证通过、Git 初始提交完成），将用户选定期刊的审稿人推荐适配版本（§1-§5）直接写入 structure/0_global/idea.md，覆盖模板内容。其中 §5 风险评估表已包含阶段五审稿人识别的 challenge（格式为：风险描述 + 等级⚠️/🔴 + 应对策略）。附加元数据不写入。如果 paper-init 中途失败，不执行此步，提示用户先解决 paper-init 的问题
```

**到此为止**，后续的文献工作流（/lit-plan → /lit-review → /lit-tag → /lit-pool）由用户在项目目录中自行启动。

## 输出文件

详见阶段六（6.1 对话框排名表 + 6.2 独立 md 文件夹）。
