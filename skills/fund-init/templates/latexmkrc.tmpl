#!/usr/bin/env perl
# fund-init 默认 latexmkrc（XeLaTeX + biber）
$pdf_mode = 5;               # 5 = xelatex
$bibtex_use = 2;
$biber = 'biber --debug';    # 如改用 bibtex，将 $bibtex_use 调整即可；$pdf_mode 与引用工具无关
$out_dir = 'build';
$clean_ext = 'bbl run.xml synctex.gz fdb_latexmk fls';
